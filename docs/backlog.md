# Article backlog

## Ready evidence

- OxideBatch vs Spring Batch 6 — four-way PostgreSQL benchmark under matched workload semantics.
- Rust vs Java가 아니었다 — separating runtime/driver observations from framework lifecycle overhead.
- Spring Batch를 Rust로 다시 만들면서 어려웠던 건 Reader/Writer가 아니었다 — restart, checkpoint, transaction, and crash boundaries.
- CI Green인데 왜 merge하지 않았나 — proof-obligation based review in AI-assisted development.
- Why an external workload repository catches failures framework unit tests do not.

## Hold until root cause is complete

- DroidVM/Gunyah guest-memory failure investigation on SM8850.
- Gen8 KGSL/GMU DCVS/RPMh/DTB tracing and undervolting feasibility.
